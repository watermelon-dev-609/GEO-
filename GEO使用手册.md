# GEO 生成式搜索优化系统 — 使用手册

> 版本 v2.2 | 适用于武汉微艺达智能科技有限公司 | 官网: http://www.weiyida.co

---

## 目录

1. [系统简介](#1-系统简介)
2. [快速上手（5分钟）](#2-快速上手5分钟)
3. [文案导入与清洗](#3-文案导入与清洗)
4. [GEO优化工坊](#4-geo优化工坊)
5. [AI评测中心](#5-ai评测中心)
6. [成果导出](#6-成果导出)
7. [策略中心](#7-策略中心)
8. [内容规范](#8-内容规范)
9. [AI收录监测](#9-ai收录监测)
10. [工作台](#10-工作台)
11. [配置API Key](#11-配置api-key)
12. [提升收录率的完整路径](#12-提升收录率的完整路径)
13. [工程师排障手册](#13-工程师排障手册)
14. [标准化作业SOP](#14-标准化作业sop)
15. [风险点与红线](#15-风险点与红线)
16. [配置文件说明（settings.yaml）](#16-配置文件说明settingsyaml)
17. [v2.1 新增功能](#17-v21-新增功能)

---

## 1. 系统简介

### 这是什么

GEO（Generative Engine Optimization）生成式搜索优化系统，帮助企业在 DeepSeek、豆包、通义千问、文心一言、Kimi、元宝、讯飞星火、Claude 等 10 大 AI 平台中获得更高的品牌曝光和内容引用。同时支持 Ollama / LM Studio 本地私有化部署。

### 和 SEO 有什么区别

| | SEO | GEO |
|------|-----|-----|
| 优化目标 | 百度/Google 搜索结果排名 | AI 大模型生成答案中的品牌引用 |
| 核心手段 | 关键词堆砌、外链建设 | 结构化内容、实体锚定、语义权威性 |
| 效果体现 | 网页在搜索结果第几位 | 品牌是否出现在 AI 回答中 |

### 核心工作流

```
文案导入 → 智能清洗 → GEO优化 → AI评测 → 成果导出
                ↑                        │
                └── 采纳建议、重新优化 ←──┘
```

### 关键术语工程化解读

| 术语 | 工程含义 | 落地写法 |
|------|----------|----------|
| **实体锚定** | 让 AI 的 NER（命名实体识别）准确提取企业名/品牌名/地名/产品名 | 企业全称首次出现即完整（"武汉微艺达智能科技有限公司"），后续可简称（"微艺达"）；地名与业务词绑定出现（"武汉沙盘定制厂家"） |
| **语义权威性** | 让 AI 的语义匹配层判定你的内容是"可信答案"而非"普通网页" | 每个核心观点配 2-3 句支撑细节 + 1 个量化数据；引用具体项目案例而非泛泛而谈；专业术语给出定义句（"数字孪生沙盘是指…"） |
| **E-E-A-T** | Google 内容质量评估框架：经验(Experience)、专业(Expertise)、权威(Authoritativeness)、可信(Trustworthiness) | 经验→展示项目落地数量/类型；专业→使用行业规范术语+技术参数标准格式；权威→标注资质认证/政企合作项目；可信→所有数据有来源依据，不编造 |
| **信源一致性** | 评测引擎将生成文本与企业官方信息交叉比对，检测 AI 幻觉 | 生成内容中的企业名/数据/案例/资质必须与原始素材完全一致；<30 分时综合评分硬上限 50 分（防幻觉核心门禁） |
| **RAG 向量检索** | AI 将文本切片为向量存入索引，用户提问时检索最相关片段再生成回答 | 每 200-300 字一个自包含信息单元；FAQ 格式天然适配 RAG 召回；技术参数写成"参数名：数值（单位）"标准格式 |
| **结构化摘要** | AI 从页面提取标题+首段作为摘要卡片展示 | 标题含地域+业务词；首段 150 字内完成"谁、在哪、做什么、服务谁"四要素；每段首句可独立被提取 |

---

## 2. 快速上手（5分钟）

### 第一步：配置 API Key

1. 点击左下角 **「配置API Key」**
2. 至少配置一个平台（推荐 DeepSeek，性价比最高）
3. 输入 API Key → 点击「保存」

### 第二步：导入文案

1. 点击侧边栏 **「文案导入」**
2. 粘贴企业文案（≥50字）→ 点击「开始智能清洗」
3. 系统自动完成清洗 + 五维信息提取

### 第三步：GEO优化

1. 点击 **「进入GEO优化工坊」**（或侧边栏进入）
2. 勾选目标 AI 平台（可多选）→ 点击「开始GEO优化」
3. 等待生成完成 → 在各平台 Tab 中查看优化结果

### 第四步：AI评测

1. 点击 **「进入AI评测中心」**
2. 确认评测文本和平台 → 点击「开始评测」
3. 查看综合评分、各维度得分、短板诊断

### 第五步：导出成果

1. 点击 **「导出报告」**（或侧边栏「成果导出」）
2. 勾选需要的导出项 → 点击「一键导出」

---

## 3. 文案导入与清洗

**路径**：侧边栏 → 文案导入

### 三种导入方式

| 方式 | 适用场景 | 操作 |
|------|----------|------|
| **文本粘贴** | 已有现成文案 | 直接粘贴 → 选择沙盘类型（可选）→ 点击「开始智能清洗」 |
| **文件上传** | Word/Markdown文档 | 拖拽文件 → 自动解析 → 点击「开始智能清洗」 |
| **快速诊断** | 先看质量再决定是否优化 | 粘贴文案 → 选择沙盘类型 → 点击「GEO健康体检」 |

### 清洗规则设置（v2.2 新增）

点击"开始智能清洗"旁的齿轮按钮 → 可独立开关 5 条清洗规则：

| 规则 | 说明 | 默认 |
|------|------|------|
| 去除冗余 | 删除重复内容、空话套话、无意义的修饰词 | ✅ |
| 术语统一 | 口语化表达规范为行业标准术语 | ✅ |
| 格式标准化 | 统一段落结构，逻辑清晰层次分明 | ✅ |
| 保留关键信息 | 产品参数、技术指标、服务流程、案例数据完整保留 | ✅ |
| 去广告化 | 去除过度营销用语，保留客观描述 | ✅ |

切换即时保存，下次清洗生效。

### 清洗结果解读

- **清洗前后对比**：系统去除广告语、冗余表述、格式噪音
- **精简率**：显示优化前后的字数变化
- **五维信息提取**：核心优势、适用场景、技术特点、服务能力、落地价值

### 快速诊断

诊断给出 5 个维度的评分（0-100）：
- **实体完整性**：企业名、地域、产品名是否完整
- **结构化程度**：标题层级、段落和列表结构
- **量化数据**：是否有可验证的数字支撑
- **FAQ友好度**：是否包含自然问答对
- **信源可信度**：内容是否有事实依据

### 沙盘类型选择指南

选择正确的沙盘类型是优化的第一步。选错类型会导致系统使用错误的行业术语和场景描述，优化结果偏离实际业务。

| 沙盘类型 | 适用文案场景 | 推荐行文结构 | 关键词侧重 |
|----------|-------------|-------------|-----------|
| **智慧交通** | 城市交通指挥中心、智慧高速、车路协同、交通规划方案 | "场景痛点→治理方案→技术实现→落地案例" | 数字化仿真、动态联动、物联网适配、交通流量模拟 |
| **智慧城市** | 城市大脑、数字政府、智慧治理、一网统管类项目 | "城市规划视野→数字治理理念→统筹能力→项目经验" | 数字孪生、大数据可视化、应急指挥、多源数据融合 |
| **智慧工业** | 智能工厂、产线仿真、工业互联网、MES系统方案 | "工业理解→技术集成→降本增效→园区案例" | 工业数字孪生、智能制造、设备互联、产线仿真 |
| **智慧农业** | 数字农业、智能灌溉、乡村振兴、农业科技示范 | "农业升级→政策对接→科技兴农→示范园区" | 数字农业、智能灌溉、精准种植、农产品溯源 |
| **智慧物流** | 智慧仓储、物流自动化、AGV调度、供应链可视化 | "效率提升→供应链优化→自动化集成→物流项目" | 智慧仓储、AGV调度、数字孪生仓、供应链可视化 |
| **军事地形** | 军事院校教学、作战推演、国防教育、军事科研 | "精准严谨→标准工艺→保密合规→专业演示" | 地形精准还原、战术仿真、比例标准化、三维地形建模 |
| **数字多媒体** | 企业展厅、科技馆、文旅体验、新品发布 | "视觉冲击→交互体验→定制创意→数字化升级" | 触控交互、声光电特效、沉浸式体验、投影融合 |
| **地产/规划/展厅** | 地产营销、城市规划展览、政府项目公示、品牌展厅 | "空间还原→展示效果→政企汇报→品牌形象" | 城市空间还原、建筑模型、灯光系统、项目规划展示 |

> **选择建议**：如果不确定选哪个，优先选择与企业主营业务最接近的类型。系统也支持不选沙盘类型直接清洗，但后续优化和评测的效果会打折扣。

---

## 4. GEO优化工坊

**路径**：侧边栏 → GEO优化工坊

### 操作步骤

1. **选择沙盘类型**：智慧交通、智慧城市、智慧工业等 8 类
2. **选择目标平台**：勾选要优化的 AI 平台（可多选）
3. **优化规则设置（v2.2 新增）**：点击齿轮按钮，按平台独立开关优化规则
4. **确认源文案**：自动从清洗结果填充，也可手动修改
5. **点击「开始GEO优化」**

### 优化规则设置（v2.2 新增，按平台独立配置）

| 平台 | 可选规则 |
|------|---------|
| **DeepSeek** | RAG切片优化、FAQ结构强化、参数标准化、品牌实体锚定、数据原词保留 |
| **豆包** | 短句硬约束(≤30字)、利益前置、通俗表达、行动指引收尾 |
| **文心一言** | 标题硬格式、首段四要素、关键词密度优化、资质背书前置 |
| **通义千问** | 三层递进结构、方案逻辑闭环、B端政企导向、交付保障体系 |
| **Kimi** | 四层递进论证、高信息密度、实体原词优先、长文深度展开 |

切换即时保存，生成文案时仅应用已启用的规则。

### 分平台最优字数建议

不同 AI 大模型对文本长度敏感度不同。系统已内置以下规则，工程师也可据此手动调整：

| 平台 | 推荐字数 | 原因 |
|------|---------|------|
| **Kimi** | 1500-2500 字 | 超长上下文优势，偏好高信息密度深度长文 |
| **DeepSeek** | 1500-2500 字 | RAG 检索需要足够的自包含信息单元，长文覆盖更多检索角度 |
| **文心一言** | 800-1500 字 | 百度搜索卡片偏好精简结构化内容 |
| **通义千问** | 800-1500 字 | B端方案型内容，过长会稀释逻辑主线 |
| **讯飞星火** | 800-1500 字 | 每段 150-300 字均衡分布，适配语音播报场景 |
| **字节豆包** | 800-1500 字 | 句长 ≤30 字，通俗短小优先 |
| **腾讯元宝** | 800-1500 字 | 政企流程型内容，过长影响关键信息提取 |
| **Claude** | 1500-3000 字 | 200K 上下文窗口，偏好结构化论证深度内容 |
| **Ollama/LM Studio** | 取决于本地模型 | 按模型能力调整，默认同 DeepSeek 规则 |

> **重要**：Kimi、DeepSeek、Claude 在系统中被标记为"长文本平台"，生成 prompt 中字数目标自动设为 1500-2500 字（Claude 为 1500-3000 字）。其余平台为 800-1500 字。Ollama / LM Studio 默认沿用 DeepSeek 规则集。

### 主流 AI 平台采信偏好对照表

| 平台 | 行文风格 | 段落长度 | 数据呈现 | FAQ 格式 | 实体植入位置 | 核心策略 |
|------|---------|---------|---------|---------|-------------|---------|
| **文心一言** | 信息密度高、结构化段落 | 200-400 字/段 | 首段含四要素 | 不强制 | 标题+首段（地域+业务词） | 百度卡片友好 |
| **通义千问** | 逻辑闭环、方案导向 | 每层 2-3 段递进 | 方案逻辑链 | 不强制 | 文首企业名+地域 | 三层结构化（理解→方案→保障） |
| **Kimi** | 深度长文、逻辑严密 | 不限长度，每段独立信息价值 | 精确数据+论据支撑 | 可选 | 标题+各层开头 | 四层递进（背景→分析→结论→依据） |
| **DeepSeek** | 技术密集、参数量化 | 200-300 字自包含单元 | 参数名：数值（单位） | **强烈推荐**（FAQ 召回率最高） | 每个信息单元独立标注 | RAG 向量检索优化 |
| **字节豆包** | 短句直白、通俗获客 | 每句 ≤30 字 | 利益前置 | 不强制 | 首段直答"能帮我做什么" | 低理解成本 |
| **讯飞星火** | 术语规范、行业深度 | 150-300 字/段 | 技术→场景→价值 | 不强制 | 行业术语+应用场景 | 垂直领域术语精准 |
| **腾讯元宝** | 正规严谨、流程清晰 | 不限 | 资质+流程+验收标准 | 不强制 | 文首企业资质+合作流程 | 政企采购风格 |
| **Claude** | 逻辑严密、论证充分 | 每段独立论证单元 | 论证→证据→结论 | 可选 | 层级标题+来源标注 | 结构化论证 |
| **Ollama/LM Studio** | 同 DeepSeek | 200-300 字 | 参数标准化 | 推荐 | 同 DeepSeek | 本地私有化部署 |

> **实操指引**：拿到优化结果后，对照上表检查关键特征是否到位。如 DeepSeek 优化结果中是否有 FAQ 对答、豆包结果每句是否 ≤30 字。

### 单平台 vs 多平台

| 模式 | 触发条件 | 特点 |
|------|----------|------|
| **流式生成** | 选择 1 个平台 | 实时显示生成过程，边生成边看 |
| **批量生成** | 选择 2+ 平台 | 逐个平台生成，显示进度条 |

### 优化结果

- **平台 Tab**：每个平台独立 Tab，切换查看
- **策略说明**：展开"优化策略说明"查看针对该平台的优化思路
- **平台对比表**：多平台时自动生成字数、策略对比

### 来自评测的重优化

评测完成后 → 点击「采纳全部建议并重新优化」→ 自动跳回工坊 → 系统自动带入评测建议 → 点击优化即可

### 来自收录监测的针对性优化

AI收录监测中 → 查看检测详情 → 点击「优化未被收录的平台」→ 自动跳回工坊 → 平台和优化指令已预填

---

## 5. AI评测中心

**路径**：侧边栏 → AI评测中心

### 评测原理

模拟真实用户在 AI 平台上的搜索行为，从 8 个维度量化评估内容的 AI 采信效果：

| 维度 | 权重 | 评估方式 | 评估内容 |
|------|------|---------|----------|
| 品牌召回 | 18 | 向量相似度（无LLM） | 品牌名能否被 AI 准确检索到 |
| 方案匹配 | 18 | 嵌入语义匹配（无LLM） | 内容与用户搜索意图的语义匹配度 |
| 优势引用 | 14 | LLM 模拟评估 | AI 是否倾向于引用企业的优势信息 |
| 真实采信 | 14 | LLM 模拟用户+实体比对 | 内容中事实数据的真实性和可信度 |
| 结构质量 | 9 | LLM 评估 | 标题层级、段落结构是否便于 AI 解析 |
| 差异化程度 | 9 | LLM 评估 | 与行业惯常水平的区分度 |
| E-E-A-T 权威度 | 10 | LLM 评估 | 企业经验、专业度、权威性、可信度信号强度 |
| 信源一致性 | 8 | LLM 交叉比对 | 内容是否偏离企业官方信息（防幻觉核心门禁） |

> **权重说明**：8 维权重合计 100。品牌召回和方案匹配权重最高（各 18），因为它们是 AI 能否"找到你"的基础。信源一致性权重最低（8），但它有特殊机制——低于 30 分时综合评分硬上限 50。

### 评测配置

1. **评测文本**：自动带入优化工坊的结果，也可手动输入
2. **沙盘类型**：匹配对应的业务类型
3. **目标平台**：选择要评测的 AI 平台
4. **模拟用户角色**：B端政企采购 / 技术人员 / 项目经理 / 普通咨询用户
5. **自定义问题**：手动输入特定问题，或点击「🤖 LLM生成评测问题」自动生成

### LLM 生成评测问题

1. 确保评测文本已填入（≥50字）
2. 点击 **「🤖 LLM生成评测问题」**
3. 系统基于文案内容生成真实搜索问题
4. 勾选需要用的问题 → 自动加入自定义问题列表
5. 可手动补充或删除

### 评测结果解读

- **综合评分**：0-100 分，80+ 为优秀，60-80 为中等，<60 需改进
- **信源一致性**：防 AI 幻觉的关键指标，<30 分时综合评分硬上限 50
- **短板诊断**：列出具体问题（单个维度 <60 分触发）
- **优化建议**：可一键采纳返回工坊重新优化
- **前后对比**：如果提供了优化前原文，自动计算改进幅度

### 评测分数波动说明

评测结果可能存在波动（同一文案多次评测分数差异 3-8 分以内属正常范围），原因及应对：

| 原因 | 说明 | 应对方式 |
|------|------|---------|
| LLM 随机性 | 评测依赖 LLM 打分，温度参数决定随机程度 | 降低 `settings.yaml` 对应维度 temperature（最低 0.1） |
| 评测问题差异 | LLM 生成的问题每次不尽相同 | 使用固定自定义问题集，多次评测取平均值 |
| 沙盘类型匹配 | 选错沙盘→行业对标基线偏移→分数偏低 | 确认沙盘类型与文案业务一致 |
| 文本长度边界 | <50 字直接评 0 分；>3000 字被截断（仅前 3000 字参与 LLM 评测） | 控制在 800-2500 字区间 |

> **校准建议**：建立企业内部基准文案（1-2 条已验证高分文案），每次怀疑评测异常时先用基准文案跑一次，对比历史分数判断是否系统异常。

### 评测历史

- 点击右上角「评测历史」→ 右侧抽屉打开
- 可展开查看历史详情
- 支持选择 2 条记录进行对比分析
- 支持删除不需要的记录

---

## 6. 成果导出

**路径**：侧边栏 → 成果导出

### 可导出内容

| 导出项 | 格式 | 说明 |
|--------|------|------|
| 优化文案 | .md | 各平台优化后的文案 |
| JSON-LD代码 | .json | Schema.org 结构化数据，用于网站SEO |
| 评测报告 | .html / .pdf | 包含雷达图、维度得分、改进建议 |
| 关键词清单 | .csv | 当前沙盘类型的关键词库 |
| 内容规范文档 | .md | AI采信六原则方法论参考 |
| 竞品调研报告 | .md | 竞品对比分析报告 |

### 操作步骤

1. 勾选需要的导出项
2. 填写企业名称和官网 URL（可选，用于 JSON-LD 生成）
3. 点击「一键导出」
4. 文件自动下载到浏览器默认下载目录

### 预览评测报告

点击「预览评测报告」→ 全屏弹窗展示 HTML 报告

---

## 7. 策略中心

**路径**：侧边栏 → 策略中心

### Tab 1：平台监测

监控 10 个 AI 平台的最新内容收录规则和算法变化。

- **查看规则**：点击平台卡片「查看详情」→ 展示规则摘要、要点、变更历史
- **AI生成摘要**：点击「AI生成摘要」→ 系统用 Web 搜索获取最新规则
- **全量检查**：点击「全量检查」→ 后台异步检查所有平台
- **自动监测**：点击「启动自动监测」→ 每 30 分钟自动检查一次
- **数据时效**：卡片上显示数据来源（Web搜索/LLM知识）和时效信息
- **手动编辑**：详情中点击「编辑规则」→ 可手动修改规则内容

### Tab 2：竞品情报

追踪竞争对手在 AI 搜索中的曝光情况。

- **添加竞品**：填写竞品名称、行业、官网、AI平台曝光、内容特征
- **引用记录**：记录在哪个平台、搜索什么词时发现竞品被引用
- **对比分析**：选择 2+ 个竞品 → 点击「对比分析」→ LLM 生成对比报告
- **导出报告**：选中竞品 → 点击「导出报告」→ 下载 Markdown 格式

### Tab 3：关键词策略

管理关键词矩阵，指导 GEO 内容改写。

- **选择沙盘类型**：点击上方的沙盘卡片
- **三个分类**：品牌词、场景词、长尾词
- **添加关键词**：手动添加或 LLM 智能扩展
- **LLM 智能扩展**：点击「LLM 智能扩展」→ 系统自动生成关联关键词
- **导入/导出 CSV**：批量管理关键词
- **权重标注**：核心词（core）、辅助词（secondary）、长尾词（longtail）

---

## 8. 内容规范

**路径**：侧边栏 → 内容规范

### Tab 1：写作模板

预设的 GEO 优化写作模板。

- **新建模板**：设置模板名称、类型、Markdown 正文（用 `{变量名}` 占位）
- **编辑/预览/删除**：鼠标悬停模板卡片可见操作按钮
- **筛选**：按类型（企业介绍/产品文案/案例模板/FAQ模板）筛选

### Tab 2：审核标准

配置内容质量的评判标准。

- **5 个维度**：实体完整性、结构化程度、量化数据、FAQ友好度、信源一致性
- **自定义权重**：拖动滑块调整各维度权重（总和建议 100%）
- **设置阈值**：每个维度的及格分数线
- **保存**：点击「保存审核标准」
- **恢复默认**：点击「恢复默认」重置

### Tab 3：使用指引

展示 GEO 内容优化的完整流程和 AI 采信六原则。

- **右侧快速导出**：勾选要导出的内容 → 点击「导出选中项」→ 下载 .md 文件

---

## 9. AI收录监测

**路径**：侧边栏 → AI收录监测

### 核心功能

实时检测品牌在 10 大 AI 平台上的被收录和引用情况。

### 使用流程（收录率提升闭环）

```
① 全量检测 → ② 查看收录矩阵 → ③ 找出未收录平台/查询
       ↑                                    ↓
       └── ⑥ 对比收录率变化 ← ⑤ 再次全量检测 ← ④ 跳转GEO工坊优化
```

### 操作步骤

1. **选择沙盘类型**：下拉选择业务类型
2. **选择查询分类**：品牌直问 / 场景问询 / 产品问询
3. **点击「全量检测」**：系统在各 AI 平台搜索品牌相关查询
4. **查看收录矩阵**：
   - 左侧卡片：每个平台的检测次数、收录次数、收录率
   - 绿色进度条：收录率 ≥50%
   - 橙色：20%-50%
   - 红色：<20%
5. **查看趋势**：右上角收录率趋势图（最近 14 天）
6. **查看详情**：点击「最近检测」中的记录 → 展开详情弹窗
   - 每行显示：查询词 → 是否被收录 → 评分 → 引用片段
   - 点击 **「优化未被收录的平台」** → 自动跳转工坊

### 如何用监测提升收录率

1. **定期检测**：每周执行一次全量检测，建立收录基线
2. **定位弱项**：找到收录率低的平台和查询类型
3. **针对性优化**：通过详情弹窗的「优化」按钮，将未收录查询转化为优化指令
4. **验证效果**：优化完成后立即再次检测，对比收录率变化
5. **持续迭代**：重复上述步骤，逐步提升所有平台的收录率

---

## 10. 工作台

**路径**：侧边栏 → 工作台（首页）

### 快速操作卡片

- **文案导入** → 进入导入页
- **GEO优化工坊** → 进入工坊
- **AI评测中心** → 进入评测
- **成果导出** → 进入导出

### 数据看板

- 累计评测次数、平均评分、优化改进率、监测维度数
- AI平台配置状态一览
- 最近评测记录（含进度条和评分）
- 评分概览（平均分 + 趋势箭头）
- 全域覆盖矩阵（10 平台优化状态）
- 最近项目记录（点击跳转）

---

## 11. 配置 API Key

**路径**：左下角「配置API Key」按钮

### 支持的平台

| 平台 | API 获取地址 |
|------|-------------|
| DeepSeek | https://platform.deepseek.com |
| 通义千问 | https://dashscope.aliyun.com |
| 文心一言 | https://yiyan.baidu.com |
| 字节豆包 | https://www.volcengine.com |
| Kimi | https://platform.moonshot.cn |
| 腾讯元宝 | https://cloud.tencent.com |
| 讯飞星火 | https://xinghuo.xfyun.cn |
| Claude | https://console.anthropic.com |
| Ollama（本地） | https://ollama.com（默认 localhost:11434） |
| LM Studio（本地） | https://lmstudio.ai（默认 localhost:1234） |

> **本地部署**：Ollama 和 LM Studio 支持完全离线运行，数据不外流，适合内网环境。在 `settings.yaml` 中设置 `local_llm.offline_mode: true` 可启用完全离线模式。

### 文心一言特殊配置

文心一言需要 **API Key + Secret Key** 双密钥。在百度智能云控制台创建应用后获取。系统中文心一言的配置项比其他平台多一个 `secret_key` 字段。

### 配置步骤

1. 在对应平台注册并获取 API Key
2. 在本系统左下角点击「配置API Key」
3. 找到对应平台，输入 API Key
4. 点击「保存」→ 立即生效

### 最低配置

至少配置 **1 个** 平台即可使用核心功能。推荐 DeepSeek（性价比最高，适合批量生成和评测）。

---

## 12. 提升收录率的完整路径

### 新用户首次使用

```
1. 配置 API Key（至少 DeepSeek）
2. 导入企业文案 → 智能清洗
3. GEO 优化工坊 → 选择平台 → 开始优化
4. AI 评测中心 → 开始评测 → 查看评分
5. 如果评分 < 60：采纳建议 → 回到工坊重新优化 → 再次评测
6. 如果评分 ≥ 70：进入成果导出 → 导出文案和 JSON-LD
7. AI收录监测 → 全量检测 → 建立收录基线
```

### 持续优化循环

```
每周：
  AI收录监测 → 全量检测 → 查看收录率变化
  ↓ 如果某个平台收录率下降
  ↓ 点击详情 → 「优化未被收录的平台」
  ↓ GEO工坊 → 针对性优化
  ↓ AI评测 → 验证内容质量
  ↓ 再次监测 → 对比效果

每月：
  策略中心 → 全量检查 → 获取平台最新规则
  竞品情报 → 添加/更新竞品 → 对比分析
  关键词策略 → LLM 智能扩展 → 更新关键词库
```

### 紧急响应（收录率突然大幅下降）

1. **策略中心** → 全量检查 → 确认是否有平台规则变更
2. **AI收录监测** → 全量检测 → 确定是哪个平台、哪类查询出了问题
3. **GEO工坊** → 针对该平台重新优化
4. **AI评测** → 验证优化效果
5. **再次监测** → 确认收录率恢复

---

## 13. 工程师排障手册

### 13.1 生成类故障

#### 生成超时 / 接口无响应

| 现象 | 原因 | 解决方案 |
|------|------|---------|
| 单平台生成超过 2 分钟无返回 | LLM API 超时（默认 120s），或模型服务拥堵 | ① 等待自动重试（最多 3 次，间隔 2 秒）；② 重试 3 次仍失败→检查 API Key 是否余额不足；③ 在 `settings.yaml` 增大 `llm.request_timeout`（最大 300s） |
| 批量生成中途卡住 | 某个平台的 API 不可用，阻塞后续平台 | 在批量进度条中查看当前卡在哪个平台→进入「配置API Key」检查该平台密钥是否有效→可跳过该平台重试 |
| 流式生成中断（SSE 断开） | 网络波动或服务端异常 | 刷新页面→检查当前会话状态（评测/优化有断线恢复机制）→如会话已标记 failed，重新发起 |

#### 同一文案多次优化结果差异过大

| 现象 | 原因 | 解决方案 |
|------|------|---------|
| 两次生成结果完全不同 | LLM 的随机性（temperature 非零） | ① 确认两次使用的平台和沙盘类型一致；② 如需结果稳定，降低对应平台的 API 调用 temperature（在 adapter 层设置）；③ 固定源文案的五维信息提取结果（如果清洗结果变了，优化结果必然不同） |
| 生成内容有编造数据 | LLM 幻觉，突破了 prompt 约束 | ① 检查源文案中是否确实缺少该信息→如果源文案没有，LLM 不应编造；② 系统有后处理校验（企业名/地域/量化数据/五维覆盖），但无法拦截所有幻觉；③ 将问题文案提交评测，信源一致性维度会揭示虚构内容 |

#### Ollama / LM Studio 本地模型无响应

| 现象 | 原因 | 解决方案 |
|------|------|---------|
| 显示"连接被拒绝" | 本地模型服务未启动 | `ollama serve` 或启动 LM Studio 的 Local Server；检查端口（Ollama 默认 11434，LM Studio 默认 1234） |
| 返回空内容或乱码 | 本地模型能力不足，不支持中文长文本 | 换用 qwen2.5 或以上级别模型；调低生成长度 |
| 生成极慢（>5 分钟） | 本地 GPU 显存不足，退化为 CPU 推理 | 换用小模型（如 qwen2.5:1.5b）；或改用云端 API |

### 13.2 评测类故障

#### 评测分数波动大 / 忽高忽低

| 现象 | 原因 | 解决方案 |
|------|------|---------|
| 同一文案 24h 内评测分差 >10 分 | LLM 评测温度偏高，或评测问题集不同 | ① 降低 `settings.yaml` 对应维度的 temperature（见 §16）；② 使用固定自定义问题集替代 LLM 动态生成；③ 连续评测 3 次取平均值 |
| 特定维度始终低于预期 | 该维度对应的内容特征确实缺失或薄弱 | 查看短板诊断→针对性改写→重新评测；参考 §4 平台对照表确认该平台的优化方向是否正确 |
| 综合评分被卡在 50 分 | 信源一致性 < 30 触发硬上限 | 检查生成文案中是否有编造的企业名/数据/案例→回工坊选择"保守模式"（遵循信源忠实原则）重新优化 |
| 品牌召回始终为 0 | 品牌名未在文本中出现或嵌入服务未加载 | 确认评测文本中确实包含完整企业名；检查 `python -c "from app.services.embedding_svc import EmbeddingService; EmbeddingService()"` 是否正常加载 |

#### 评测生成问题为空 / 质量差

| 现象 | 原因 | 解决方案 |
|------|------|---------|
| LLM 生成问题返回空列表 | API 调用失败或返回格式异常 | 检查默认平台 API Key 是否有效；系统有 fallback 机制（自动从预设问题集补充） |
| 生成的问题与文案内容无关 | LLM 未充分理解文案 | 确保评测文本 ≥50 字且信息完整；手动补充自定义问题作为兜底 |

### 13.3 系统类故障

#### 前端页面白屏 / 加载失败

| 现象 | 原因 | 解决方案 |
|------|------|---------|
| 页面空白，控制台报错 | 前端构建缓存或依赖问题 | `cd frontend && rm -rf node_modules/.vite && npx vite --port 5173 --force` |
| API 请求全部 500 | 后端服务未启动或崩溃 | `cd backend && python run.py`；查看 `backend/data/logs/app.log` 排查 |
| 数据丢失（评测历史/关键词等消失） | JSON 文件被误删或损坏 | 检查 `backend/data/` 目录各文件是否存在；从备份恢复（如有） |

#### 后端启动失败

| 现象 | 原因 | 解决方案 |
|------|------|---------|
| `ModuleNotFoundError` | 依赖未安装 | `cd backend && pip install -r requirements.txt` |
| `Port 8000 already in use` | 上次进程未退出 | `netstat -ano | findstr :8000` → `taskkill /PID <PID> /F` |
| `HF_MIRROR` 相关错误 | Windows 下 HuggingFace 模型下载失败 | 设置环境变量 `HF_ENDPOINT=https://hf-mirror.com`；或手动下载模型到 `data/cache/models` |
| `KMP_DUPLICATE_LIB_OK` 警告 | Windows + PyTorch 多库冲突 | 设置环境变量 `KMP_DUPLICATE_LIB_OK=TRUE`（不影响功能） |

### 13.4 数据类故障

#### CSV 导出乱码 / 格式异常

| 现象 | 原因 | 解决方案 |
|------|------|---------|
| Excel 打开 CSV 中文乱码 | 编码问题（系统使用 UTF-8） | 用记事本打开→另存为→编码选"UTF-8 with BOM"；或直接用 WPS/LibreOffice 打开 |
| 关键词 CSV 逗号错位 | 关键词本身含逗号 | v2.0 已修复（添加 `csv.QUOTE_NONNUMERIC`），如仍出现请升级到最新版 |

#### 评测历史无法加载

| 现象 | 原因 | 解决方案 |
|------|------|---------|
| 评测历史列表为空 | JSON 文件损坏或权限问题 | 检查 `backend/data/evaluations/` 目录是否存在且可读写；尝试删除 `evaluations_index.json` 让系统重建索引 |
| 部分历史记录缺失 | 内存中的会话未持久化到磁盘 | 系统在查询历史时会自动补保存内存会话；如仍缺失，系会话状态异常被清理 |

---

## 14. 标准化作业SOP

### 14.1 单条文案多轮优化迭代标准步骤

此为工程师日常处理客户文案的标准操作流程，建议严格执行以保障交付质量。

```
【第1轮：基线建立】
1. 导入原始文案 → 智能清洗 → 记录清洗前后字数、精简率
2. 选择目标平台（通常 3-5 个核心平台）→ 开始GEO优化
3. AI评测（使用默认问题集）→ 记录综合评分和维度得分（作为基线）
4. 查看短板诊断 → 记录评分 <60 的维度 → 导出第1轮评测报告

【第2轮：短板修复】
5. 点击「采纳全部建议」→ 系统自动跳回工坊
6. 确认优化指令已带入（在策略说明中可看到本轮需解决的问题清单）
7. 开始重优化 → 等待生成完成
8. AI评测（使用相同问题集确保可比性）→ 对比第1轮分数
9. 如果评分提升 ≥5 分且 ≥65 分 → 可交付
   如果评分提升 <5 分或仍 <60 → 进入第3轮

【第3轮：深度优化（如需）】
10. 分析第2轮仍低于60的维度 → 手动补充优化指令
    （如"结构质量"低 → 手动指令"增加H2/H3层级标题，每段配要点列表"）
11. 切换评测用户角色再评测（如"技术人员选型"换成"B端政企采购"）
12. 如果3轮后评分仍不达标 → 标记该文案为"需人工介入"，
    可能原因：原始素材信息量不足、行业术语缺失、缺乏量化数据

【交付标准】
- 综合评分 ≥65 分（80+ 优秀交付，65-80 标准交付，<65 需标注）
- 信源一致性 >30 分（否则硬上限触发，综合分不可能高）
- 品牌召回 >40 分（品牌名必须在文本中被检测到）
```

### 14.2 收录率低时的标准处置流程

```
【定位阶段】
1. AI收录监测 → 全量检测 → 导出最新收录矩阵
2. 确定问题范围：
   - 单个平台收录率低 → 该平台规则可能已变更 → 策略中心查看该平台最新规则
   - 某类查询收录率低（如场景问询） → 内容缺少该类查询的匹配信息
   - 全局收录率低 → 品牌基础信息未有效嵌入 → 检查所有平台文案的企业名/地域是否完整

【修复阶段】
3. 针对问题平台 → GEO工坊 → 选择该平台单独优化
4. 优化时重点关注：平台规则变更项、该平台特有的收录偏好
5. AI评测验证 → 确保综合评分 ≥65

【验证阶段】
6. 全量检测 → 对比收录率变化
7. 如果收录率提升 ≥10% → 记录优化方案为有效策略
   如果未提升 → 开始下一轮"定位→修复→验证"循环
8. 每次验证间隔建议 ≥1 小时（AI 平台的内容更新有延迟）
```

### 14.3 批量文案作业标准

```
【作业前准备】
1. 确认目标平台清单（建议 ≤5 个平台以控制耗时）
2. 确认沙盘类型（同一批次用同一类型，避免类型切换）
3. 预估耗时：单条文案"清洗→优化→评测"约 3-8 分钟（视平台数和文本长度）

【作业顺序】
4. 优先处理核心产品/服务文案（企业官网"解决方案"页面的文案）
5. 次优先处理场景化文案（针对特定应用场景的文案）
6. 最后处理辅助文案（FAQ、案例、技术参数页）

【抽检规则】
7. 每 5 条文案抽检 1 条做完整评测（8 维全量）
8. 其余可做快速评测（仅跑品牌召回+方案匹配 2 个无 LLM 维度，速度快）
9. 抽检不合格（<60 分）→ 该批次全部进入复检

【批量导出】
10. 确认所有文案评测 ≥65 分 → 批量导出（优化文案 .md + JSON-LD .json + 评测报告 .html）
11. 按"平台_沙盘类型_日期"命名文件夹
```

### 14.4 客户交付包标准格式

```
交付文件夹结构：
├── 01_优化文案/
│   ├── {企业名}_DeepSeek优化文案_20260601.md
│   ├── {企业名}_豆包优化文案_20260601.md
│   ├── {企业名}_通义千问优化文案_20260601.md
│   └── ...（每个目标平台一个文件）
├── 02_结构化数据/
│   └── {企业名}_JSON-LD_20260601.json
├── 03_评测报告/
│   ├── {企业名}_综合评测报告_20260601.html
│   └── {企业名}_维度对比雷达图_20260601.png（如有）
├── 04_关键词清单/
│   └── {沙盘类型}_关键词库_20260601.csv
└── 05_交付说明.md（含：优化日期、目标平台、综合评分、收录基线、下次复测建议日期）

交付说明模板：
---
## 交付信息
- 交付日期：2026-06-01
- 优化平台：DeepSeek、豆包、通义千问、文心一言、Kimi
- 沙盘类型：智慧交通沙盘
- 综合评分：DeepSeek(78) / 豆包(72) / 通义千问(81) / 文心一言(75) / Kimi(69)
- 收录基线：待 7 天后首次全量检测建立

## 建议
- 下次复测日期：2026-06-08（7天后执行全量检测建立收录基线）
- 重点关注平台：Kimi（评分 <70，建议 2 周后针对性重优化）
---
```

---

## 15. 风险点与红线

### 15.1 禁止操作（可能导致数据不可逆丢失）

| 禁止操作 | 后果 | 正确做法 |
|----------|------|---------|
| **直接删除 `backend/data/` 目录** | 所有评测历史、关键词库、平台规则、竞品数据、模板全部丢失 | 如需清理，仅删除特定子文件（如 `evaluations/` 下的过期评测），保留索引文件 |
| **批量清空平台规则** | 所有平台的优化规则被重置为系统默认，无法恢复自定义编辑 | 编辑规则前先导出备份；使用"恢复默认"而非"全部清空" |
| **全量覆盖关键词库** | CSV 导入的"覆盖模式"会删除已有关键词，替换为新内容 | 导入前确认选择"追加模式"；导入前先 CSV 导出做备份 |
| **删除历史评测记录（大量）** | 无法回溯效果变化趋势，客户汇报缺乏数据支撑 | 按月归档而非删除；可移至 `data/evaluations/archive/` 子目录 |
| **手动编辑 JSON 数据文件** | JSON 格式错误导致系统读取失败，数据损坏 | 必须编辑时，先复制备份 → 用 JSON 校验工具验证格式 → 再替换原文件 |
| **在优化过程中关闭浏览器/刷新** | SSE 流式连接中断，会话状态可能标记为异常 | 等待生成完成或点击取消；如已中断，通过评测历史的断线恢复机制找回 |

### 15.2 内容红线（触发 AI 判定为广告/虚假信息，直接影响收录与评分）

| 红线类型 | 具体表现 | 为什么危险 | 合规写法 |
|----------|---------|-----------|---------|
| **无依据的对比/排名声明** | "行业领先""优于同行 30%""国内第一""唯一通过XX认证" | AI 会判定为不可信营销语言，引用率归零；信源一致性维度直接扣分 | 去掉比较级；有数据→写具体数据并标注来源；无数据→写"专注于""致力于" |
| **编造量化数据** | 服务年限、项目数量、团队规模、技术参数等虚构的数字 | 信源一致性检查会交叉比对，发现虚构直接 <30 分，触发综合评分硬上限 50 | 只使用原始素材中已存在的数字；没有就不写；可用"多年""众多"等定性描述替代 |
| **编造客户/项目案例** | "服务过XX集团""某市政府项目""与XX合作"等虚构合作 | 严重的企业信息造假，信源一致性+真实采信双重扣分 | 只写原始素材中明确列出的客户和项目 |
| **虚假资质/认证** | "通过ISO9001认证""高新技术企业"等未实际获得的资质 | 评测时 LLM 可能识别出该资质与行业常识不符→EEAT 权威度直接判低分 | 核实后只填写真实持有的资质；不确定的不写 |
| **过度承诺** | "100% 满意""保证排名""绝对收录"等绝对化用语 | 触发 AI 内容安全审核机制，可能导致内容被平台降权或屏蔽 | 用"致力于""力求""持续优化"等务实表述 |
| **关键词堆砌** | 同一关键词在文中反复出现 ≥10 次，密度 >5% | 百度等搜索引擎识别为 SEO 作弊，AI 平台同理会降低引用优先级 | 关键词密度控制在 3-5%，同义词轮换使用 |
| **竞品攻击/贬低** | "XX公司技术落后""竞品质量差"等贬损竞品的内容 | 不仅违反广告法，AI 也会判定为低质量对比内容，降低采信 | 如需对比，只做客观的技术参数对照，标注数据来源 |

### 15.3 操作风险

| 风险点 | 影响 | 预防措施 |
|--------|------|---------|
| 多个工程师同时操作同一数据文件 | JSON 文件并发写入可能导致数据损坏 | 系统有原子写入机制（tempfile+os.replace），但不支持多进程并发；建议单机单人操作 |
| 配置错误的 API Key | LLM 调用全部失败，优化/评测不可用 | 配置后点击"保存"→立即做一次快速评测验证（仅品牌召回维度，不消耗 LLM 额度） |
| 评测温度设置过低（<0.1） | LLM 输出几乎完全确定，评测失去区分度 | 不建议将任何维度 temperature 设为 0，最低 0.1 |
| 本地模型离线模式未正确配置 | offline_mode=true 但本地模型未启动→所有优化/评测失败 | 启动本地模型后先在"配置API Key"中测试连接 |

---

## 16. 配置文件说明（settings.yaml）

**路径**：`geo-optimizer/backend/config/settings.yaml`

### 16.1 常用可修改参数

#### LLM 平台配置（`llm` 节点）

| 参数 | 默认值 | 可修改范围 | 修改后影响 |
|------|--------|-----------|-----------|
| `llm.default_model` | `"deepseek"` | 任意已配置平台名 | 评测/优化默认使用的 AI 平台（未指定平台时的 fallback） |
| `llm.request_timeout` | `120` | 30-300（秒） | LLM API 请求超时时间。网络慢或使用本地模型时可调大，最大 300s |
| `llm.max_retries` | `3` | 1-5 | API 调用失败后的自动重试次数 |
| `llm.retry_delay` | `2` | 1-10（秒） | 重试间隔。频繁触发限流时调大 |
| `llm.platforms.<平台>.enabled` | `false` | `true` / `false` | 控制该平台是否在前端可选。设为 `false` 时前端不显示 |

#### 评测配置（`evaluation` 节点）

| 参数 | 默认值 | 可修改范围 | 修改后影响 |
|------|--------|-----------|-----------|
| `evaluation.questions_per_role` | `10` | 5-20 | 每次评测每个用户角色生成的问题数。值越大评测越全面，但耗时越长 |
| `evaluation.similarity_threshold` | `0.65` | 0.5-0.9 | 语义匹配阈值。低于此值的文本块不会被判定为"匹配"。值越高匹配越严格 |
| `evaluation.temperature.advantage_citation` | `0.3` | 0.1-1.0 | 优势采信评测的 LLM 温度。越低结果越确定（推荐 0.2-0.4） |
| `evaluation.temperature.structure_quality` | `0.3` | 0.1-1.0 | 结构质量评测的 LLM 温度 |
| `evaluation.temperature.differentiation` | `0.3` | 0.1-1.0 | 差异化程度评测的 LLM 温度 |
| `evaluation.temperature.eeat` | `0.3` | 0.1-1.0 | E-E-A-T 权威度评测的 LLM 温度 |
| `evaluation.temperature.real_citation` | `0.5` | 0.2-1.0 | 真实采信评测的 LLM 温度。略高以模拟真实用户引用行为的多样性 |
| `evaluation.temperature.source_consistency` | `0.2` | 0.1-0.5 | 信源一致性评测的 LLM 温度。**建议不调高**——这是防幻觉门禁，高温会漏判 |

> **温度调节原则**：温度越低，LLM 回答越确定、一致；温度越高，回答越多样、随机。评测维度建议 0.2-0.4，真实采信可到 0.5（模拟真人搜索行为的自然多样性），信源一致性建议维持 0.2。

#### 文本处理配置（`text_processing` 节点）

| 参数 | 默认值 | 可修改范围 | 修改后影响 |
|------|--------|-----------|-----------|
| `text_processing.min_cleaned_length` | `50` | 30-200 | 清洗后最小字符数。低于此值的文案系统拒绝处理（视为无效输入） |
| `text_processing.max_chunk_size` | `2000` | 1000-4000 | 文本切片最大字符数。影响向量检索精度，不建议随意改动 |
| `text_processing.chunk_overlap` | `200` | 100-500 | 切片重叠字符数。确保信息单元不跨切片断裂 |

#### 本地大模型配置（`local_llm` 节点）

| 参数 | 默认值 | 可修改范围 | 修改后影响 |
|------|--------|-----------|-----------|
| `local_llm.provider` | `"ollama"` | `"ollama"` / `"lmstudio"` | 本地模型服务类型 |
| `local_llm.base_url` | `"http://localhost:11434"` | 任意有效 URL | 本地模型服务地址。LM Studio 默认 `http://localhost:1234/v1` |
| `local_llm.model_name` | `"qwen2.5"` | 本地已安装的模型名 | 本地使用的模型。推荐 qwen2.5 或以上支持中文的模型 |
| `local_llm.offline_mode` | `false` | `true` / `false` | 完全离线模式。开启后所有 LLM 调用走本地模型，外部 API 不可用 |

#### 品牌监测配置（`brand_monitor` 节点）

| 参数 | 默认值 | 可修改范围 | 修改后影响 |
|------|--------|-----------|-----------|
| `brand_monitor.brand_variants` | 企业全称+简称列表 | 任意品牌名列表 | 收录监测时使用的品牌搜索词。建议覆盖全称+常用简称 |
| `brand_monitor.llm_verify_threshold` | `60` | 40-80 | LLM 判定"已收录"的置信度阈值。越低越敏感（可能误报），越高越严格（可能漏报） |

#### v2.2 新增配置节点

**清洗规则配置（`cleaning` 节点）**:
```yaml
cleaning:
  rules:
    remove_redundancy: {enabled: true, label: "去除冗余", description: "..."}
    unify_terminology: {enabled: true, label: "术语统一", description: "..."}
    standardize_format: {enabled: true, label: "格式标准化", description: "..."}
    preserve_key_info: {enabled: true, label: "保留关键信息", description: "..."}
    de_advertise: {enabled: true, label: "去广告化", description: "..."}
```
通过前端 UI 或 API 修改，也可直接编辑 YAML 后重启。

**优化规则配置（`optimization` 节点）**:
```yaml
optimization:
  platforms:
    deepseek:
      rules:
        rag_chunking: {enabled: true, label: "RAG切片优化", ...}
        faq_structure: {enabled: true, label: "FAQ结构强化", ...}
        ...
    doubao: {rules: {...}}
    wenxin: {rules: {...}}
    tongyi: {rules: {...}}
    kimi: {rules: {...}}
```
每个平台独立规则集，通过前端 UI 或 API 修改。

**企业官网配置（`system` 节点）**:
```yaml
system:
  enterprise_website: "http://www.weiyida.co"
```
UTM 推广计划创建时自动填入。

### 16.2 禁止修改的核心配置项

以下配置项修改后可能导致系统行为异常或数据损坏，**除非明确知道影响，否则不要修改**：

| 禁止修改项 | 原因 |
|-----------|------|
| `system.data_dir` | 数据目录路径变更后，所有历史数据无法读取 |
| `system.enterprise_name` | 企业名是信源一致性的基准，变更后评测结果全部失效 |
| `llm.platforms.<平台>.base_url` | 除非平台官方 API 地址变更，否则修改后调用失败 |
| `llm.platforms.<平台>.model_name` | 模型名与平台 API 不匹配会导致调用失败 |
| `embedding.*` | 嵌入模型参数变更后，历史向量数据失效，品牌召回评测结果不可比 |
| `auth.*` | 鉴权配置变更后可能导致无法登录或安全漏洞 |
| `storage.local_only` | 变更后可能触发敏感数据外流 |

### 16.3 修改配置后如何生效

- **大部分配置**：重启后端服务生效（`cd backend && python run.py`）
- **API Key 配置**：通过前端"配置API Key"页面修改，保存后立即生效（不绑定 settings.yaml 文件）
- **评测温度配置**：修改 `settings.yaml` 后重启后端生效

### 16.4 配置示例

```yaml
# 示例：调整为"保守评测"模式（评测更稳定，但区分度略降）
evaluation:
  temperature:
    advantage_citation: 0.15    # 接近确定性评测
    structure_quality: 0.15
    differentiation: 0.15
    eeat: 0.15
    real_citation: 0.3         # 保留一定随机性模拟真实用户
    source_consistency: 0.1    # 最严格，几乎不允许变异
```

---

## 17. v2.1 新增功能

### 17.1 动态规则引擎 — 模板编辑器

**访问**: 导航至「模板引擎」（`/template-engine`）

**背景**: 之前平台规则硬编码在 Python 代码中，改规则需要改代码+重启。现在每个平台的优化规则是一个独立的 YAML 配置文件（`data/platform_templates/{platform}.yaml`），可通过可视化界面编辑，60 秒自动生效。

**操作流程**:
1. 左侧选择目标 AI 平台
2. 中间 7 个 Tab（概览/Header/Body/Schema/Weights/验证/Rules）编辑各组件
3. 右侧实时预览内容结构
4. 点「校验」确认配置完整 → 点「保存」生效
5. 右侧面板查看版本历史，支持 Diff 对比和回滚

**关键配置项**:

| 组件 | 说明 | 示例 |
|------|------|------|
| `header.title_format` | 标题模板 | `{location}+{business}+厂家` |
| `header.first_paragraph_rules` | 首段规则列表 | 150字内完成四要素、结论先行 |
| `body.faq_count` | FAQ 数量范围 | `{min: 3, max: 5}` |
| `body.paragraph_length` | 段落字数范围 | `{min: 100, max: 300}` |
| `weights.content_sources` | 内容源权重 | `{百家号: 9, 搜狐: 6, 知乎: 4}` |
| `schema.preferred_types` | 优先 Schema 类型 | `["Article", "Product"]` |
| `verification.forbidden_words` | 平台特有禁词 | 行业领先、国内首创 |

### 17.2 RSS 信源监控 + AI 采信测试

**RSS 监控**（策略中心 → 平台监测 → RSS 检查）:
- 7 大信源每日自动抓取：百度搜索/百家号/文心博客/头条/豆包/微信/知乎小红书
- 关键词告警：抓取、索引、收录、权重、算法更新
- 结果持久化到 `data/rss_monitor/`，可按日期查询

**AI 采信测试**（策略中心 → 采信测试）:
- 26 条行业高频问题 × 5 大 AI 平台
- 四维分析：被引站点、结构特征、时效偏好、拒采模式
- 结果持久化到 `data/citation_tests/`

**结构变化周报**（策略中心 → 结构报告）:
- 每周自动生成《AI 抓取结构变化报告》
- 含：RSS 告警摘要、采信测试洞察、结构调整建议、竞品动态、行动清单

### 17.3 适配流水线

**访问**: 导航至「适配流水线」（`/adaptation`）

**用途**: 当平台规则变化时，自动化完成从扫描到发布的 11 阶段流程。

**操作流程**:
```
创建运行 → 扫描存量 → 重生成 → 校验 → 灰度10% → 观察 → 全量 → 3天/7天测试
```

**关键能力**:
- **灰度发布**: 先发 10% 内容，观察 AI 引用变化，确认无误再全量
- **一键回滚**: 适配失败可立即切回旧模板配置
- **自动校验**: 首段规范/H标签层级/FAQ存在/Schema/禁词/合规 全部自动检查
- **发布后验证**: 3 天和 7 天后自动执行采信测试验证效果

### 17.4 数据闭环仪表盘

**访问**: 导航至「数据闭环」（`/feedback`）

**核心指标**:

| 指标 | 计算方式 |
|------|---------|
| AI 采信率 | 内容在 5 大 AI 中的被引用次数 / 总检索次数 × 100% |
| 结构命中率 | 匹配预期结构特征的被引用次数 / 总引用次数 × 100% |
| 时效衰减率 | 发布后 7/15/30/90 天的引用率变化幅度 |
| 违规拒采率 | 因广告/堆砌/虚假数据被拒采的比例 |

**自动诊断**:
1. 检测采信率是否下降（与上周对比，下降 >10% 触发告警）
2. 回溯本周监控报告定位可能原因
3. 定位具体结构问题（FAQ 不足/段落过长/缺少结论先行 等）
4. 生成迭代建议和行动步骤

**定时任务**: 每周一 08:00 自动计算各平台周指标

---

## 18. v2.2 新增功能

### 18.1 清洗规则可配置开关

**访问**: 文案导入页面 → 「清洗设置」按钮

- 5 条清洗规则各自独立开关（去除冗余/术语统一/格式标准化/保留关键信息/去广告化）
- 切换即时保存到 `settings.yaml` → `cleaning.rules` 节点
- 清洗时仅应用已启用的规则构建 LLM prompt

### 18.2 GEO优化规则按平台独立配置

**访问**: GEO优化工坊 → 勾选目标平台 → 「优化规则设置」按钮

- 5 个平台（DeepSeek/豆包/文心/通义/Kimi）各 4-5 条规则独立开关
- 切换即时保存到 `settings.yaml` → `optimization.platforms.<平台>.rules` 节点
- 生成文案时仅应用已启用的规则，禁用的规则以禁用指令注入 prompt

### 18.3 转化追踪与UTM归因

**访问**: 侧边栏 → UTM推广计划管理、全链路漏斗

**落地页配置**: 系统默认官网 `http://www.weiyida.co`（`settings.yaml` → `system.enterprise_website`）

**UTM 推广计划**:
1. 创建推广计划 → 系统自动为每个 AI 平台生成带 UTM 追踪参数的链接
2. UTM 参数: `utm_source=deepseek&utm_medium=ai_referral&utm_campaign=计划名`
3. 将带 UTM 的链接发布到官网对应页面

**转化事件上报**:
```
POST /api/conversions/event
{"type":"form_submit","value":5000,"landing_page":"...","referrer":"utm_source=deepseek..."}
```
系统自动解析 UTM 参数并归因到具体 AI 平台。

**全链路漏斗**: AI曝光→AI引用→网站访问→转化，四阶段转化率自动计算。

**接入真实数据（可选）**:
- GA4: 配置服务账号凭据到 `data/traffic/config.json`
- 百度统计: 配置 api_key/secret_key/site_id
- 不接外部数据源时，UTM 链接生成和 AI 漏斗估算仍可正常使用

### 18.4 官网默认配置

`settings.yaml` → `system.enterprise_website` = `"http://www.weiyida.co"`
UTM 推广计划创建时落地页 URL 自动填入，无需每次手填。

---

## 附录：常见问题

### Q：优化后评测分数没有提升？
A：检查"短板诊断"中的具体问题是否在重优化时被解决。在工坊中点击「采纳全部建议」确保优化指令被带入。如果 2 轮后仍无提升，参考 [§14.1 单条文案多轮优化迭代标准步骤](#141-单条文案多轮优化迭代标准步骤)。

### Q：AI收录监测显示"未配置"？
A：该平台尚未配置 API Key。点击左下角「配置API Key」添加。

### Q：JSON-LD 代码怎么用？
A：将生成的 JSON-LD 代码嵌入网站 `<head>` 中，用 `<script type="application/ld+json">` 包裹。

### Q：可以同时优化多个平台吗？
A：可以。在工坊中勾选多个平台，系统会逐个生成，每个平台都有独立的优化策略。

### Q：评测的 8 个维度可以自定义权重吗？
A：可以。在评测中心的「评测维度」区域勾选/取消维度，拖动滑块调整权重。评测温度（控制 AI 回答的确定性与多样性）也可在 `settings.yaml` 的 `evaluation.temperature` 中按维度独立配置。

### Q：为什么同样的文案评两次分数不一样？
A：LLM 评测存在固有随机性，差异在 3-8 分内属正常。如需稳定结果：① 降低对应维度的 temperature；② 使用固定自定义问题集；③ 连续测 3 次取平均。详见 [§5 评测分数波动说明](#评测分数波动说明)。

### Q：生成超时了怎么办？
A：系统会自动重试 3 次（间隔 2 秒）。如果 3 次都失败，检查：① API Key 余额是否充足；② 网络是否可达；③ 可在 `settings.yaml` 增大 `llm.request_timeout`。详见 [§13 工程师排障手册](#13-工程师排障手册)。

### Q：哪些平台适合同时优化？
A：推荐按场景分组——B端政企组（通义千问+元宝+文心一言），技术深度组（DeepSeek+Kimi+Claude），大众传播组（豆包+讯飞星火）。详见 [§4 平台采信偏好对照表](#主流-ai-平台采信偏好对照表)。

### Q：信源一致性一直很低怎么解决？
A：这说明生成内容中可能掺杂了 AI 编造的信息。回到 GEO 工坊，在源文案区域手动勾选"严格信源模式"（如果前端支持），或在优化前手动删除源文案中不存在的任何"看起来专业"的编造数据。核心原则：**宁可少写，不可错写**。

### Q：平台规则变了怎么快速适配？（v2.1）
A：① 确认告警（策略中心 → RSS 检查）；② 编辑模板（模板引擎 → 选择平台 → 调整配置 → 保存）；③ 执行适配（适配流水线 → 创建运行 → 扫描 → 灰度10% → 观察 → 全量）。全程无需改代码、无需重启。

### Q：如何知道平台规则变了？（v2.1）
A：系统自动通过 3 个渠道感知：① 7 大 RSS 信源每日抓取 + 关键词告警；② 每周一 AI 采信行为自动化测试；③ 每周一自动生成《AI 抓取结构变化报告》。访问策略中心查看最新告警和报告。

### Q：灰度发布怎么操作？（v2.1）
A：适配流水线中点击「灰度10%」→ 系统先发布 10% 内容 → 等待 1-2 天观察采信率变化 → 正常则「全量发布」→ 异常则「回滚」。

### Q：本地模型（Ollama/LM Studio）和云端 API 怎么选？
A：涉密/内网环境 → 本地模型；追求效果 → 云端 API（DeepSeek 性价比最高）。本地模型推理质量受限于模型大小，建议至少 qwen2.5:7b 才能产出可用的优化文案。

---

> 📞 技术支持：查看 CLAUDE.md 获取技术架构和开发文档
> 📅 最后更新：2026-06-10（v2.2 清洗/优化规则可配置化 + 转化追踪更新）
